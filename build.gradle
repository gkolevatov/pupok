ext {
    libraries = [
            scala_sdk: 'org.scala-lang:scala-library:2.13.4',
            thrift: 'org.apache.thrift:libthrift:0.13.0'
    ]

    testLibraries = [
            junit    : 'junit:junit:4.12',
            scalatest: 'org.scalatest:scalatest_2.13:3.2.3',
            flexmark : 'com.vladsch.flexmark:flexmark-all:0.35.10'
    ]
}

class DockerExtension {
    def message = 'Hello from GreetingPlugin'
}

class DockerPrepareTask extends DefaultTask {
    def source
    def destination

    @InputFile
    File getSource() {
        project.file(source)
    }

    @OutputFile
    File getDestination() {
        project.file(destination)
    }

}
